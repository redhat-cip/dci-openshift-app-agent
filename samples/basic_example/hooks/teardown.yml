- name: Get logs from pods
  include_role:
    name: get-logs-from-namespace
  vars:
    get_logs_ns: "{{ dci_openshift_app_ns | default('myns') }}"
    get_logs_folder: "{{ job_logs.path }}"

- name: Delete myns
  k8s:
    api_version: v1
    kind: Namespace
    name: "{{ dci_openshift_app_ns | default('myns') }}"
    state: absent
