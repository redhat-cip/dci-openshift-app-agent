apiVersion: v1
kind: Service
metadata:
  app: httpd
  name: webserver
spec:
  ports:
  - name: webserver1
    port: 80
    protocol: TCP
    targetPort: 8080
  type: NodePort
  ipFamilyPolicy: PreferDualStack
  selector:
    app: httpd
